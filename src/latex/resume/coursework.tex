[~ if (r.education.history && r.education.history.length) 
{ ~]
\cvsection{Relevant Coursework}

%Mechanical Control Systems
%Matlab for Engineers	\\
%Heat Transfer 
%Mechanical Engineer Labs  \\
%Design of Mechanical Components 
%Mechanical Engineering Measurements \\
%Elements of Electrical Engineering         % Mechanics of Materials\\
%Engineering Graphics \& CAD 
%Optics \\
%Heat Transfer 
%Computational Mechanics
%Mechanics of Materials
%Optics
%Fluid Mechanics
%Design Mech Compnts
%Cad in mech engg
%Drafting
%Computational Mechanics
%Engineering Economics
%Thermodynamics 
%Mech props of matls
%Vibrations And Controls 
\begin{cvcourses}
  [~ for( let cou of r.education.history ) { 
      console.log(Object.keys(cou).map(function(key){return cou[key]}));
      var courses = cou.curriculum;
      console.log(typeof(courses));
      console.log(courses);
      courses.shift;
      console.log(courses);
      var len_cou = courses.length;
      function isCourse(cou){
        if( isNaN(cou) == false ){
          return '';
        }
        else{
          return cou;
        }
      }

      var i = 0;
      while( false ){ 
        if( courses === undefined || i === 5)
        {break;}
        if( courses.length === len_cou ){
          print('\\cvcourse'); 
        }
        else{
          var c = [courses.shift,courses.shift,courses.shift];
          print( '{' + isCourse( c[0] ) + '}'
                +'{' + isCourse( c[1] ) + '}'
                +'{' + isCourse( c[2] ) + '}');
        }
      ~]
    [~ } ~]
  [~ }); ~]
\end{cvcourses}

[~ } ~]
